# internal/monocypher

> **Statut :** Fichier oracle C transpilé d'origine (`a_linux_amd64.go`), généré mécaniquement par `ccgo`.  
> **Dérogation `go vet` :** Ce paquet est gelé et sert uniquement d'oracle de référence C pour la suite de tests KAT différentiels (`kat/kat_test.go`). Les avertissements `unsafe.Pointer` émis par `go vet` proviennent du générateur automatique `ccgo` et sont une propriété connue du code transpilé brut C-vers-Go, sans impact sur le moteur optimisé `c2simd`.
